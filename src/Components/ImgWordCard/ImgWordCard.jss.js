import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(
    {
        root: {
            display: "inline-block",
            // margin: '0.25rem',
        },
        img: {
            width: "100%",
        },
    },
    { index: 1 }
);
