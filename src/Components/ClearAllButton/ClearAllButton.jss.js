import makeStyles from "@mui/styles/makeStyles";

export const useStyles = makeStyles(
    (theme) => ({
        clearAllButton: {
            marginRight: "auto",
        },
    }),
    { index: 1 }
);
