import makeStyles from "@mui/styles/makeStyles";

export const useStyles = makeStyles(
    (theme) => ({
        body: {
            display: "flex",
            flexWrap: "wrap",
        },
    }),
    { index: 1 }
);
